# Fixtures intentionally skipped by the reference generator.
#
# Format:
#   <relative-path-under-tests/>  TAB  <reason>
#
# Lines starting with `#` and blank lines are ignored.
# Rust-side byte-exact tests must also skip these — they have no
# paired tests/reference/.svg and never will, until the underlying
# cause changes upstream.
#
# 2026-05-02: cleared all entries to expose every remaining gap via
# sweep_all. ELK fixtures are still filtered out programmatically by
# `is_elk()` in sweep_all.rs / generate_ref.mjs (opt-in non-goal),
# not via this list.
#
# 2026-05-03 (Wave 14 后): re-add 6 environmental / non-fixable
# fixtures. These cannot reach byte-exact without porting host
# environment quirks rather than upstream logic.

# gantt timezone × 4 — V8 `new Date()` 解析畸形日期串、DST 边界、
# `%s` epoch 处理与任何 Rust 时间库都不同；要 byte-exact 需自带
# V8 行为日期解析器（不值得）。
ext_fixtures/cypress/gantt/05	V8 new Date() timezone quirk
ext_fixtures/cypress/gantt/39	V8 new Date() timezone quirk
ext_fixtures/demos/gantt/06	V8 new Date() timezone quirk
ext_fixtures/demos/gantt/07	V8 new Date() timezone quirk

# demos/class/08 — 上游 fixture 文本与 jison 接受集自相矛盾
# （port 之前就坏）；除非改 fixture 或 grammar 否则无法 byte-exact。
ext_fixtures/demos/class/08	upstream fixture/jison contradiction

# demos/venn/04 — 4-set venn 触发 constrainedMDS 迭代优化器，
# 收敛轨迹依赖 V8 Math.random() 调用时刻 PRNG 状态，不共享。
ext_fixtures/demos/venn/04	constrainedMDS depends on V8 PRNG state
