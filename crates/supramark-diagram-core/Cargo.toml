[package]
name = "supramark-diagram-core"
version = "0.1.1"
edition = "2021"
rust-version = "1.82"
license = "Apache-2.0"
homepage = "https://github.com/Actrium/supramark"
repository = "https://github.com/Actrium/supramark"
description = "Unified DiagramEngine trait and public EngineAst envelope that converge supramark's four diagram engines."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
